class Name {
	constructor( a ) {
		this._a = a
	}
	// GETTERS
	get a() { return this._a }
	// SETTERS
	set a( a ) {
		this._a = a
	}
	// METHODS
	method( arg ) {
		//console.info(`method(${arg})`)
    // code
	}
}