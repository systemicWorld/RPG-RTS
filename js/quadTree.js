class Quadtree {
	constructor( a, b, c ) {
		this._a = a
		this._ = 
		this._ = 
	}
	// GETTERS
	get a() { return this._a }
	get () { return this._ }
	get () { return this._ }
	// SETTERS
	set a( a ) {
		this._a = a
	}
	set (  ) {
		this._ = 
	}
	set (  ) {
		this._ = 
	}
	// METHODS
	method( arg ) {
		//console.info(`method(${arg})`)
    	// code
	}
}